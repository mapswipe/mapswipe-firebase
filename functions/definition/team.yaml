FbTeam:
  model: alias
  # path: teams/{teamId}
  docs: Represents a team to limit project visibility.
  type:
    type: object
    fields:
      teamName:
        type: string
      teamToken:
        type: timestamp
      isArchived:  # NOTE: This was added on revamp
        type: boolean

